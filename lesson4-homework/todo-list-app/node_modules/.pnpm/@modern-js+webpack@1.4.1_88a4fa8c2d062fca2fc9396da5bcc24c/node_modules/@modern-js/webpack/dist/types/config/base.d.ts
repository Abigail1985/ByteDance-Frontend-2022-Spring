import Chain from 'webpack-chain';
import { IAppContext, NormalizedConfig } from '@modern-js/core';
import { BabelChain } from '@modern-js/babel-chain';
export declare type ResolveAlias = {
  [index: string]: string;
};

declare class BaseWebpackConfig {
  chain: Chain;
  appContext: IAppContext;
  metaName: string;
  options: NormalizedConfig;
  appDirectory: string;
  dist: string;
  jsFilename: string;
  jsChunkname: string;
  cssChunkname: string;
  mediaChunkname: string;
  babelChain: BabelChain;
  isTsProject: boolean;
  constructor(appContext: IAppContext, options: NormalizedConfig);
  name(): void;
  mode(): "development" | "production";
  devtool(): void;
  entry(): void;
  output(): void;
  publicPath(): string;
  loaders(): Chain.Rule<Chain.Module>;
  plugins(): void;
  resolve(): void;
  cache(): void;
  optimization(): void;
  stats(): void;
  config(): any;
  getChain(): Chain;
}

export { BaseWebpackConfig };