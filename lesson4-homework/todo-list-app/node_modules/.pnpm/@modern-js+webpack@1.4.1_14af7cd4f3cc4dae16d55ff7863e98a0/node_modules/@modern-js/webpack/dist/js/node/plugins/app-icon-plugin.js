"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.AppIconPlugin = void 0;

var _fs = _interopRequireDefault(require("fs"));

var _path = _interopRequireDefault(require("path"));

var _webpack = require("webpack");

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

class AppIconPlugin {
  constructor(htmlWebpackPlugin, iconPath) {
    this.htmlWebpackPlugin = void 0;
    this.iconPath = void 0;
    this.name = void 0;
    this.htmlWebpackPlugin = htmlWebpackPlugin;
    this.iconPath = iconPath;
    this.name = 'app-icon';
  }

  apply(compiler) {
    const {
      publicPath
    } = compiler.options.output;

    const iconName = _path.default.basename(this.iconPath); // add html asset tags


    compiler.hooks.compilation.tap(this.name, compilation => {
      this.htmlWebpackPlugin.getHooks(compilation).alterAssetTagGroups.tap(this.name, data => {
        data.headTags.unshift({
          tagName: 'link',
          voidTag: true,
          attributes: {
            rel: 'apple-touch-icon',
            sizes: '180*180',
            href: `${publicPath}${iconName}`
          },
          meta: {}
        });
        return data;
      });
    }); // copy icon to dist

    compiler.hooks.thisCompilation.tap('app-icon', compilation => {
      compilation.hooks.processAssets.tap({
        name: 'app-icon',
        stage: _webpack.Compilation.PROCESS_ASSETS_STAGE_PRE_PROCESS
      }, assets => {
        const source = _fs.default.readFileSync(this.iconPath);

        assets[iconName] = new _webpack.sources.RawSource(source, false);
      });
    });
  }

}

exports.AppIconPlugin = AppIconPlugin;