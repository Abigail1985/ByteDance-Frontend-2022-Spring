export declare const EN_LOCALE: {
  environment: {
    node_version: string;
    nvm_install: string;
    yarn_pnpm_npm: string;
  };
  install: {
    failed: string;
    failed_no_command: string;
    success: string;
  };
  git: {
    failed: string;
    success: string;
  };
  templated: {
    failed: string;
  };
  generator: {
    failed: string;
  };
  success: {
    info: string;
  };
};