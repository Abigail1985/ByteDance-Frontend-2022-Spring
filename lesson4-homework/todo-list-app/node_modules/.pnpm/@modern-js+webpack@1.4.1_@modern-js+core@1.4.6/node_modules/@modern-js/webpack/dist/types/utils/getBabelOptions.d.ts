import { NormalizedConfig } from '@modern-js/core';
import { Options as BabelPresetAppOptions } from '@modern-js/babel-preset-app';
import type { BabelChain } from '@modern-js/babel-chain';
export declare const getBabelOptions: (metaName: string, appDirectory: string, config: NormalizedConfig, name: string, chain: BabelChain) => {
  babelrc: boolean;
  configFile: boolean;
  cacheIdentifier: string;
  cacheDirectory: string;
  cacheCompression: boolean;
  compact: boolean;
  presets: (string | BabelPresetAppOptions)[][];
};