import _regeneratorRuntime from "@babel/runtime/regenerator";

function asyncGeneratorStep(gen, resolve, reject, _next, _throw, key, arg) { try { var info = gen[key](arg); var value = info.value; } catch (error) { reject(error); return; } if (info.done) { resolve(value); } else { Promise.resolve(value).then(_next, _throw); } }

function _asyncToGenerator(fn) { return function () { var self = this, args = arguments; return new Promise(function (resolve, reject) { var gen = fn.apply(self, args); function _next(value) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "next", value); } function _throw(err) { asyncGeneratorStep(gen, resolve, reject, _next, _throw, "throw", err); } _next(undefined); }); }; }

import lintStaged from 'lint-staged';
import { logger, chalk } from '@modern-js/utils';

var printFormattedMsg = function printFormattedMsg(success) {
  logger.log('');
  logger.log('==============================');
  logger.log('');

  if (success) {
    logger.log('[PRECOMMIT] Verified!');
    logger.log('Please commit all autofixes caused by eslint, stylelint and prettier');
  } else {
    logger.log('[PRECOMMIT] failed!');
    logger.log('');
    logger.log('For TRULY URGENT case (like hotfix), you can skip the pre-commit checking:');
    logger.log('git commit --no-verify -m "message"');
    logger.log('Otherwise, you should solve all above issues one by one');
  }

  logger.log('');
};

export default /*#__PURE__*/_asyncToGenerator( /*#__PURE__*/_regeneratorRuntime.mark(function _callee() {
  var success;
  return _regeneratorRuntime.wrap(function _callee$(_context) {
    while (1) {
      switch (_context.prev = _context.next) {
        case 0:
          _context.prev = 0;
          _context.next = 3;
          return lintStaged();

        case 3:
          success = _context.sent;
          printFormattedMsg(success);
          _context.next = 10;
          break;

        case 7:
          _context.prev = 7;
          _context.t0 = _context["catch"](0);
          logger.error(chalk.red(_context.t0));

        case 10:
        case "end":
          return _context.stop();
      }
    }
  }, _callee, null, [[0, 7]]);
}));