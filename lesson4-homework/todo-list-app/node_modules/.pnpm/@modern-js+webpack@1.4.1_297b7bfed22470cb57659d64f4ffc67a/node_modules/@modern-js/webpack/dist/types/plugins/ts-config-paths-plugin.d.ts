import { MatchPath } from 'tsconfig-paths';
import type { Resolver } from './plugin.type';
export declare class TsConfigPathsPlugin {
  source: string;
  target: string;
  cwd: string;
  compilerOptions: any;
  absoluteBaseUrl: string;
  matchPath: MatchPath;
  resolved: Map<string, string | undefined>;
  constructor(cwd: string);
  apply(resolver: Resolver): void;
}